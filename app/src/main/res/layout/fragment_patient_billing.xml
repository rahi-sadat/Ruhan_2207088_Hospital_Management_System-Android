<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F2F3F4">

    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No approved bills or appointments found."
        android:textColor="#7F8C8D"
        android:textSize="16sp"
        android:gravity="center"
        android:padding="20dp"
        android:visibility="visible" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvBills"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/layoutCheckoutArea"
        android:padding="4dp" />

    <LinearLayout
        android:id="@+id/layoutCheckoutArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        android:orientation="vertical"
        android:padding="16dp"
        android:elevation="10dp">

        <TextView
            android:id="@+id/tvTotalPendingAmount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Total Due: 0.0 TK"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#E74C3C"
            android:gravity="center"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/btnCheckout"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Checkout"
            android:enabled="false"
            android:backgroundTint="#95A5A6"
            android:textSize="16sp"
            android:textStyle="bold"/>
    </LinearLayout>
</RelativeLayout>